---
import ExportPanel from './ExportPanel.astro';
import SiteNavbar from '../web/SiteNavbar.astro';

interface Props {
  label?: string;
}

const { label = 'Imprimir hoja' } = Astro.props;

const paperOptions = [
  { id: 'letter', label: 'Carta' },
  { id: 'a5', label: 'A5' },
  { id: 'a6', label: 'A6' },
] as const;
---

<SiteNavbar>
  <div
    slot="paper"
    class="join"
    role="radiogroup"
    aria-label="Papel"
    data-testid="paper-size-selector"
  >
    {
      paperOptions.map((option) => (
        <button
          class={
            option.id === 'letter'
              ? 'join-item btn btn-sm btn-active btn-primary'
              : 'join-item btn btn-sm'
          }
          type="button"
          role="radio"
          aria-checked={option.id === 'letter' ? 'true' : 'false'}
          data-paper-size-option={option.id}
        >
          {option.label}
        </button>
      ))
    }
  </div>
  <button slot="download" class="btn btn-primary" type="button" data-testid="download-pdf">
    <span data-download-label>Descargar PDF</span>
  </button>
  <button slot="print" class="btn btn-ghost" type="button" onclick="window.print()">
    {label}
  </button>
  <button slot="options" class="btn" type="button" data-testid="export-options">
    Opciones
  </button>
</SiteNavbar>
<ExportPanel />

<script>
  import { bindPaperToolbar } from '../../scripts/paper-size';
  import { bindPdfExport } from '../../scripts/export-pdf';

  bindPaperToolbar();
  bindPdfExport();
</script>
